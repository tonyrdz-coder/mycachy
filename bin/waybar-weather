#!/bin/bash

result=$(curl -sf --max-time 5 "wttr.in/?format=%c+%t" 2>/dev/null | tr -d '\n')

if [[ -n "$result" ]]; then
  printf '{"text":"%s","tooltip":"Weather from wttr.in"}\n' "$result"
else
  printf '{"text":"","class":"unavailable"}\n'
fi
